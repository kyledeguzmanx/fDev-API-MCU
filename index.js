const GSheetReader = require("g-sheets-api");
const config = require("./config")
GSheetReader(
    options,
    (results) => {
        //results instructions
    },
    (error) => {
        //error instructions
    }
);